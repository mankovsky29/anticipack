@page "/packing-activities"
@using Anticipack.Components.Shared.DialogComponent
@using Anticipack.Storage
@using Anticipack.Packing
@using Anticipack.Services
@using Microsoft.Extensions.Localization
@using Anticipack.Resources.Localization
@inject IPackingRepository PackingRepository
@inject NavigationManager Navigation
@inject IDialogService DialogService
@inject ILocalizationService LocalizationService
@inject IStringLocalizer<AppResources> Localizer
@implements IDisposable

<div class="page-container with-fixed-navbar">
    @if (_isLoading)
    {
        <div class="loading">
            <div class="loading-spinner"></div>
            <span>@Localizer["Loading"]</span>
        </div>
    }
    else
    {
        <div class="sticky-header">
            <div class="search-bar @(_isSearchVisible ? "visible" : "hidden")">
                <input type="text" class="search-input" placeholder="@Localizer["SearchPlaceholder"]" @bind="_searchTerm" @bind:event="oninput" />
                <button class="search-close-btn" @onclick="ToggleSearchBar">
                    <i class="fa fa-times"></i>
                </button>
            </div>

            <!-- Filter Bar -->
            <div class="filter-bar">
                <div class="filter-chips">
                    <button class="filter-chip @(_shareFilter == ShareFilterType.All ? "active" : "")" 
                            @onclick="() => SetShareFilter(ShareFilterType.All)">
                        <i class="fa fa-layer-group"></i>
                        <span>@Localizer["All"]</span>
                    </button>
                    <button class="filter-chip @(_shareFilter == ShareFilterType.Private ? "active" : "")" 
                            @onclick="() => SetShareFilter(ShareFilterType.Private)">
                        <i class="fa fa-lock"></i>
                        <span>@Localizer["Private"]</span>
                    </button>
                    <button class="filter-chip @(_shareFilter == ShareFilterType.Shared ? "active" : "")" 
                            @onclick="() => SetShareFilter(ShareFilterType.Shared)">
                        <i class="fa fa-share-alt"></i>
                        <span>@Localizer["Shared"]</span>
                    </button>
                </div>
            </div>
        </div>
        
        @if (_activities.Any())
        {
            <div class="activities-list">
                @foreach (var activity in FilteredActivities)
                {
                    <div class="activity-card">
                        <div class="activity-header">
                            <div class="activity-title-row">
                                <h3 @onclick="() => OpenActivityInEditMode(activity.Id)"  class="activity-name">@activity.Name</h3>
                                @if (activity.IsShared)
                                {
                                    <div class="share-badge" title="@Localizer["SharedActivity"]">
                                        <i class="fa fa-share-alt"></i>
                                    </div>
                                }
                            </div>
                        </div>

                        <div class="activity-info" @onclick="() => OpenActivityInEditMode(activity.Id)">
                            <div class="activity-date">
                                <i class="fa fa-calendar"></i> @Localizer["LastPacked"] @activity.LastPacked.ToString("MMM d, yyyy")
                            </div>
                        </div>
                        
                        <div class="activity-actions">
                            <button class="action-btn play" @onclick="() => OpenActivityInPackingMode(activity.Id)" title="@Localizer["PackNow"]">
                                <i class="fa fa-play"></i>
                            </button>
                            <button class="action-btn share @(activity.IsShared ? "shared" : "")" 
                                    @onclick="() => ToggleActivityShare(activity)" 
                                    title="@(activity.IsShared ? Localizer["MakePrivate"] : Localizer["MakeShared"])">
                                <i class="fa @(activity.IsShared ? "fa-lock-open" : "fa-share-alt")"></i>
                            </button>
                            <button class="action-btn delete" @onclick="() => PromptDeleteActivity(activity)" title="@Localizer["Delete"]">
                                <i class="fa fa-trash"></i>
                            </button>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div class="no-activities">
                <div class="empty-icon">
                    <i class="fa fa-luggage-cart"></i>
                </div>
                <h3>@Localizer["NoActivities"]</h3>
                <p>@Localizer["NoActivitiesDescription"]</p>
                <button class="btn primary" @onclick="AddNewActivity">
                    <i class="fa fa-plus"></i> @Localizer["CreateActivity"]
                </button>
            </div>
        }
        
        <!-- Speed Dial FAB -->
        <div class="speed-dial @(_isFabExpanded ? "expanded" : "")">
            <div class="fab-backdrop @(_isFabExpanded ? "visible" : "")" @onclick="ToggleFab"></div>
            
            <div class="fab-options">
                <button class="fab-option search-fab @(_isFabExpanded ? "visible" : "")" @onclick="OpenSearchBar" title="@Localizer["Search"]">
                    <i class="fa fa-search"></i>
                </button>
                <button class="fab-option add-fab @(_isFabExpanded ? "visible" : "")" @onclick="AddNewActivity" title="@Localizer["AddActivity"]">
                    <i class="fa fa-plus"></i>
                </button>
            </div>
            
            <button class="main-fab" @onclick="ToggleFab" aria-label="Actions menu">
                <i class="fa @(_isFabExpanded ? "fa-times" : "fa-ellipsis-v")"></i>
            </button>
        </div>
    }
</div>

@if (_showAddDialog)
{
    <div class="modal-overlay" @onclick="CancelAddActivity">
        <div class="modal-content" @onclick:stopPropagation>
            <h4 class="modal-header">@Localizer["NewPackingActivity"]</h4>
            
            <div class="form-row">
                <label for="activity-name">@Localizer["Name"]</label>
                <input id="activity-name" @bind="_newActivityName" />
            </div>

            <div class="form-row checkbox-row">
                <label class="checkbox-label">
                    <input type="checkbox" @bind="_newActivityIsShared" />
                    <span class="checkbox-text">
                        <i class="fa fa-share-alt"></i>
                        @Localizer["MakeShared"]
                    </span>
                </label>
                <p class="help-text">@Localizer["SharedActivityDescription"]</p>
            </div>

            <div class="form-actions">
                <button class="btn cancel" @onclick="CancelAddActivity">@Localizer["Cancel"]</button>
                <button class="btn primary" @onclick="ConfirmAddActivityAsync">@Localizer["Create"]</button>
            </div>
        </div>
    </div>
}

<div class="toast-container" aria-live="polite" aria-atomic="true">
    <div class="@($"toast {(_showToast ? "show" : "")}")">@_toastMessage</div>
</div>

@code {
    private enum ShareFilterType
    {
        All,
        Private,
        Shared
    }

    private List<Storage.PackingActivity> _activities = new();
    private bool _isLoading = true;
    private string _searchTerm = string.Empty;
    private bool _isSearchVisible = false;
    private bool _isFabExpanded = false;
    private ShareFilterType _shareFilter = ShareFilterType.All;

    // Add dialog
    private bool _showAddDialog;
    private string _newActivityName = string.Empty;
    private bool _newActivityIsShared = false;

    // Toast
    private bool _showToast;
    private string _toastMessage = string.Empty;
    private System.Threading.CancellationTokenSource? _toastCts;

    protected override void OnInitialized()
    {
        LocalizationService.CultureChanged += OnCultureChanged;
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadActivitiesAsync();
    }

    private void OnCultureChanged(object? sender, System.Globalization.CultureInfo culture)
    {
        InvokeAsync(StateHasChanged);
    }

    private void ToggleFab()
    {
        _isFabExpanded = !_isFabExpanded;
    }

    private void OpenSearchBar()
    {
        _isSearchVisible = true;
        _isFabExpanded = false;
    }

    private void ToggleSearchBar()
    {
        _isSearchVisible = !_isSearchVisible;
        if (_isSearchVisible)
        {
            _isFabExpanded = false;
        }
    }

    private void SetShareFilter(ShareFilterType filterType)
    {
        _shareFilter = filterType;
    }

    private async Task LoadActivitiesAsync()
    {
        _isLoading = true;
        StateHasChanged();

        try
        {
            _activities = await PackingRepository.GetAllAsync();
            _activities = _activities.OrderByDescending(a => a.LastPacked).ThenBy(a => a.RunCount).ToList();
        }
        catch
        {
            ShowToast(Localizer["FailedToLoad"]);
        }
        finally
        {
            _isLoading = false;
            StateHasChanged();
        }
    }

    private IEnumerable<Storage.PackingActivity> FilteredActivities
    {
        get
        {
            var filtered = _activities.AsEnumerable();
            
            if (!string.IsNullOrWhiteSpace(_searchTerm))
            {
                filtered = filtered.Where(a => a.Name.Contains(_searchTerm, StringComparison.OrdinalIgnoreCase));
            }

            // Apply share filter
            filtered = _shareFilter switch
            {
                ShareFilterType.Private => filtered.Where(a => !a.IsShared),
                ShareFilterType.Shared => filtered.Where(a => a.IsShared),
                _ => filtered
            };
            
            return filtered;
        }
    }

    private void AddNewActivity()
    {
        _newActivityName = "New Packing Activity";
        _newActivityIsShared = false;
        _showAddDialog = true;
        _isFabExpanded = false;
    }

    private void CancelAddActivity()
    {
        _showAddDialog = false;
    }

    private async Task ConfirmAddActivityAsync()
    {
        if (string.IsNullOrWhiteSpace(_newActivityName))
            return;

        var newActivity = new Storage.PackingActivity
        {
            Name = _newActivityName,
            LastPacked = DateTime.Now,
            IsShared = _newActivityIsShared
        };

        try
        {
            await PackingRepository.AddOrUpdateAsync(newActivity);
            ShowToast(Localizer["ActivityCreated"]);
            await LoadActivitiesAsync();
        }
        catch
        {
            ShowToast(Localizer["FailedToCreate"]);
        }
        finally
        {
            _showAddDialog = false;
        }
    }

    private async Task ToggleActivityShare(Storage.PackingActivity activity)
    {
        var newShareState = !activity.IsShared;
        var confirmMessage = newShareState 
            ? string.Format(Localizer["ConfirmMakeShared"], activity.Name)
            : string.Format(Localizer["ConfirmMakePrivate"], activity.Name);
        
        var result = await DialogService.ShowConfirmAsync(
            Localizer["ChangeShareStatus"], 
            confirmMessage,
            Localizer["Confirm"],
            Localizer["Cancel"]);
        
        if (!result) 
            return;
            
        activity.IsShared = newShareState;
        
        try
        {
            await PackingRepository.AddOrUpdateAsync(activity);
            var message = activity.IsShared 
                ? string.Format(Localizer["ActivityNowShared"], activity.Name)
                : string.Format(Localizer["ActivityNowPrivate"], activity.Name);
            ShowToast(message);
            await LoadActivitiesAsync();
        }
        catch
        {
            ShowToast(Localizer["FailedToUpdateShareStatus"]);
        }
    }

    private async Task PromptDeleteActivity(Storage.PackingActivity activity)
    {
        var message = string.Format(Localizer["DeleteActivityMessage"], activity.Name);
        var result = await DialogService.ShowConfirmAsync(Localizer["DeleteActivityTitle"], message);
        
        if (!result) 
            return;
            
        await DeleteActivityAsync(activity);
    }

    private async Task DeleteActivityAsync(Storage.PackingActivity activity)
    {
        try
        {
            await PackingRepository.DeleteAsync(activity.Id);
            ShowToast(Localizer["ActivityDeleted"]);
            await LoadActivitiesAsync();
        }
        catch
        {
            ShowToast(Localizer["FailedToDelete"]);
        }
    }

    private void OpenActivityInPackingMode(string id)
    {
        Navigation.NavigateTo($"/packing-activity?id={id}&mode=packing");
    }

    private void OpenActivityInEditMode(string id)
    {
        Navigation.NavigateTo($"/packing-activity?id={id}&mode=edit");
    }

    private void ShowToast(string message)
    {
        try
        {
            _toastCts?.Cancel();
            _toastCts?.Dispose();
        }
        catch { }

        _toastCts = new System.Threading.CancellationTokenSource();

        _toastMessage = message;
        _showToast = true;
        StateHasChanged();

        _ = HideToastAfterDelayAsync(_toastCts.Token);
    }

    private async Task HideToastAfterDelayAsync(System.Threading.CancellationToken token)
    {
        try
        {
            await Task.Delay(2000, token);
            _showToast = false;
            await InvokeAsync(StateHasChanged);
        }
        catch (System.Threading.Tasks.TaskCanceledException)
        {
        }
    }

    public void Dispose()
    {
        LocalizationService.CultureChanged -= OnCultureChanged;
        _toastCts?.Cancel();
        _toastCts?.Dispose();
    }
}
